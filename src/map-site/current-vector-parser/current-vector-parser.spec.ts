import { CurrentVectorParser } from './current-vector-parser';

describe('CurrentVectorParser', () => {
  it('should create an instance', () => {
    expect(new CurrentVectorParser()).toBeTruthy();
  });
});
